import React from "react";
import styled from "styled-components";

const StyledHorizontalSection = styled.section`
  display: flex;
  flex-direction: row;

  margin: 0;
  padding: 0;
`;

export default StyledHorizontalSection;
